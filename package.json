{
  "name": "patas-na-rua-fullstack",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Plataforma full stack para gestão de animais, adoções, necessidades e doações.",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test --test-concurrency=1",
    "check": "node --check src/server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
